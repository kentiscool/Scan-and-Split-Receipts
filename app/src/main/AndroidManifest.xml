<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kent.split2">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ChooseLoginRegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".ShowCaptureActivity" />
        <activity android:name=".FindUsersActivity" />
        <activity android:name=".ChooseReceiverActivity" />
        <activity android:name=".ReviewRecieptActivity1" />
        <activity android:name=".ReviewRecieptActivity2" />
        <activity android:name=".ReviewReceiptActivity3" />
        <activity android:name=".ReceiptActivity" />
        <activity android:name=".ReviewReceiptActivity"></activity>
    </application>

</manifest>